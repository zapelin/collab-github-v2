			for (RemoteSystemCommit commit : commits) {
				User commitAuthor = mappingService.getCollabUserOrAdmin(commit.getAuthorName(), remoteSystem);
				try {
					processCommit(
							review,
							commitAuthor,
							commit,
							repo,
							fileInfos.get(commit),
							fileNames);
					log.info("added commit " + commit.getHash11() + " to " + review);
				} catch (Exception e) {
					review.delete();
					throw e;
				}
			}